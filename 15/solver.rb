class Robot
  def command(move)
    # If no boxes, just move
    # If boxes, attempt to move boxes
    # If no space, do nothing
  end

  def move(direction)
    # Move robot in direction specified
  end

  def move_boxes(direction)
    # Check a direction for number of boxes in a row
    # Move all boxes in a row, one item
  end
end
